export const publicRoutes = ['/']

export const authRoutes = ['/login', '/register']

export const protectedRoutes = ['/settings', '/workout']

export const apiAuthPrefix = '/api/auth'

export const DEFAULT_LOGIN_REDIRECT = '/settings'
