{
  "name": "gatorfit-backend",
  "version": "1.0.0",
  "description": "Express backend for GatorFit",
  "main": "src/server.ts",
  "scripts": {
    "clean": "rimraf dist",
    "start": "npm-run-all --parallel watch:build watch:server --print-label",
    "watch:build": "tsc --watch",
    "watch:server": "nodemon \"./dist/server.js\" --watch \"./dist\""
  },
  "author": "Will Pearce et al.",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1",
    "nodemon": "^3.1.7",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  }
}
